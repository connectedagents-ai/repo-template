version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule: { interval: weekly }
  # Uncomment what the repo uses:
  # - package-ecosystem: npm
  #   directory: /
  #   schedule: { interval: weekly }
  # - package-ecosystem: pip
  #   directory: /
  #   schedule: { interval: weekly }
